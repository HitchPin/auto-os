build --incompatible_disallow_empty_glob=false
query --incompatible_disallow_empty_glob=false

# Import Aspect bazelrc presets
import %workspace%/bazelrcs/.aspect/aspect.bazelrc

# Import Custom settings
import %workspace%/bazelrcs/modern.bazelrc

import %workspace%/.apko/.bazelrc

# honor the setting of `skipLibCheck` in the tsconfig.json file
common --@aspect_rules_ts//ts:skipLibCheck=honor_tsconfig


build --experimental_isolated_extension_usages
query --experimental_isolated_extension_usages
run --experimental_isolated_extension_usages