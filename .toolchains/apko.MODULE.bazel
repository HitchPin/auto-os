bazel_dep(name = "rules_apko", version = "1.2.3")

apko = use_extension("@rules_apko//apko:extensions.bzl", "apko")

apko.translate_lock(
    name = "runner_lock",
    lock = "//infra/runner-container-image:image.lock.json",
)
use_repo(apko, "runner_lock")