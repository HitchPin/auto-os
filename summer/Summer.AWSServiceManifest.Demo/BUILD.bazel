load(
    "@rules_dotnet//dotnet:defs.bzl",
    "csharp_binary",
)

SOURCES = glob(
    include = [
        "*.cs",
    ],
    allow_empty = False,
    exclude = [],
)

csharp_binary(
    name = "bin",
    srcs = SOURCES,
    out = "Summer.AWSServiceManifest.Demo",
    target_frameworks = ["net8.0"],
    visibility = ["//visibility:public"],
    deps = [
        "//summer/Summer.AWSServiceManifest:lib",
    ],
)
