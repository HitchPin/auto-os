load(
    "@rules_dotnet//dotnet:defs.bzl",
    "csharp_library"
)

SOURCES = glob(
    include = ["*.cs", "Floyd/*.cs"],
    allow_empty = False,
    exclude = [],
)

csharp_library(
    name = "lib",
    out = "Summer.AWSServiceManifest",
    srcs = SOURCES,
    target_frameworks = ["net8.0"],
    deps = [
        "@paket.main//pulumi"
    ],
    visibility = ["//visibility:public"],
)