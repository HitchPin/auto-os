load(
    "@rules_dotnet//dotnet:defs.bzl",
    "csharp_library",
)

SOURCES = glob(
    include = ["*.cs"],
    allow_empty = False,
    exclude = [],
)

csharp_library(
    name = "lib",
    srcs = SOURCES,
    out = "Summer.Environment",
    target_frameworks = ["net8.0"],
    visibility = ["//visibility:public"],
    deps = [
        "@paket.main//microsoft.extensions.logging.abstractions",
        "@paket.main//pulumi",
    ],
)
