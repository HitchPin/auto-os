load(
    "@rules_dotnet//dotnet:defs.bzl",
    "csharp_library"
)

SOURCES = glob(
    include = ["*.cs"],
    allow_empty = False,
    exclude = [],
)

csharp_library(
    name = "lib",
    out = "Summer.Environment.KnownTokens",
    srcs = SOURCES,
    target_frameworks = ["net8.0"],
    deps = [
        "//summer/Summer.Environment:lib"
    ],
    visibility = ["//visibility:public"],
)