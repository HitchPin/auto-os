## Setup

1. (Optional but recommended) [Install direnv](https://direnv.net/). This tool hooks into your shell and will automatically load environment variables
from $PWD/.envrc, which means you don't have to worry about loading Nix each time you work on this project!