import { Construct } from 'constructs';
export interface AosCdkInternalProps {
}
export declare class AosCdkInternal extends Construct {
    constructor(scope: Construct, id: string, props?: AosCdkInternalProps);
}
