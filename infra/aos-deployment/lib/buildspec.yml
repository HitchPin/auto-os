version: 0.2
  
phases:   
  install:
    runtime-versions:
      nodejs: 18

  pre_build:
    commands:
      - admin
      
  build:
    commands:
      - pnpm build
      
  post_build:
    commands:
      - cdk deploy --method="prepare-change-set" --change-set-name=$CHANGE_SET_NAME